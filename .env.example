# Strava API Credentials
# Get these from https://www.strava.com/settings/api

VITE_STRAVA_CLIENT_ID=your_client_id_here
VITE_STRAVA_CLIENT_SECRET=your_client_secret_here

# Optional: Override redirect URI (defaults to window.location.origin)
# VITE_STRAVA_REDIRECT_URI=http://localhost:5173

# Mapbox Token
# Get your token from https://account.mapbox.com/
# Free tier: 50,000 map loads/month
VITE_MAPBOX_TOKEN=your_mapbox_token_here

# Strapi CMS (powers /blog)
# Use the public URL of your Strapi instance, e.g. Strapi Cloud or self-hosted.
# A read-only API token is optional — only needed if you set the public
# Article find/findOne endpoints to require authentication.
VITE_STRAPI_URL=https://your-strapi-instance.example.com
# VITE_STRAPI_TOKEN=your_read_only_api_token
