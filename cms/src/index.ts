export default {
  /**
   * `register` runs before app initialization. Use it to extend or replace
   * the configuration of plugins, services, and content types.
   */
  register(/* { strapi } */) {},

  /**
   * `bootstrap` runs after Strapi has fully started. Use it to seed data,
   * register custom permissions, or kick off background work.
   */
  bootstrap(/* { strapi } */) {},
};
